#include <main.h>

/**
 * _strlen -> to get length of string 
 * @s: string pointer
 * Return: length of the string
 */

int _strlen(char *s);

{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		continue;
	}
	return (i)
}	
